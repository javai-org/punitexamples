# Reference Specs

Curated snapshots of specs generated by PUnit measure experiments.

The working copies in `../specs/` are regenerated on every experiment run and
are gitignored. These reference copies are committed to show what measure
experiments produce. Update them deliberately when baselines change materially.

# Reference Baselines

Curated snapshots of baseline files generated by PUnit measure experiments.

The working copies in `../baselines/` are regenerated on every experiment run
and are gitignored. These reference copies are committed to show what measure
experiments produce. Update them deliberately when baselines change materially.
